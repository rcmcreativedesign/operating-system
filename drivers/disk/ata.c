/*
Check controller's status register
Detect if the controller exists at all
Reset/Initialize it
Detect if none, one, or two devices attached to each ATA controller
Detect if hard disk, CD ROM
Determine transfer mode, size, etc.
Error handling

*/